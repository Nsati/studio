
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // WARNING: These rules are wide open for development/demo purposes
    // to allow a password-less admin panel.
    // Do not use these in a production environment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
