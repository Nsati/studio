
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    //----------------------------------------------------------------------
    // Helper Functions
    //----------------------------------------------------------------------

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    //----------------------------------------------------------------------
    // Collection Rules
    //----------------------------------------------------------------------

    match /users/{userId} {
      // A user can only create, read, update and delete their own profile.
      allow read, write: if isOwner(userId);

      match /bookings/{bookingId} {
        // A user can only manage their own bookings.
        allow read, write: if isOwner(userId);
      }
    }
    
    // Public collections are read-only for everyone.
    // Writes would need to be handled by backend code or more specific rules.
    match /cities/{cityId} {
      allow read: if true;
      allow write: if false;
    }

    match /hotels/{hotelId} {
      allow read: if true;
      allow write: if false;

      match /rooms/{roomId} {
        allow read: if true;
        allow write: if false;
      }
    }
  }
}
